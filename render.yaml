services:
  - type: web
    name: pocket-option-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    runtime: python3.10
    envVars:
      - key: PORT
        value: 10000
